import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:nadir_app/views/sdevice.dart';
import 'package:nadir_app/views/sdlist.dart';

void main() {
  group('Test of adding, editting & deleting entrys', () {
    testWidgets('Adding an new entry', (WidgetTester tester) async {
    });

    // testWidgets('Editting an entry', (WidgetTester tester) async {
    // });

    // testWidgets('Deletting an entry', (WidgetTester tester) async {
    // });

  });
}